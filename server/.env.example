# Copy this file to `.env` and fill the values before running in production.

# Preferred SMTP configuration (recommended)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM="IGDTUW Lost & Found <no-reply@example.com>"

# Backwards-compatible Gmail (use app password if using Gmail)
# EMAIL=youremail@gmail.com
# EMAIL_PASS=your_app_password

# MongoDB connection string
# MONGO_URI=mongodb://localhost:27017/lostandfound

# NODE_ENV=development
